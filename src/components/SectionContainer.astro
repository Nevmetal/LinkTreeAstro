---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-20 mx-auto container  max-w-md flex flex-col items-center `}
>
  <slot />
</section>
